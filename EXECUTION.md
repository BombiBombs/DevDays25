Entregable nivel 0
es solo a nivel de backend por tanto `npm run dev` y tener encendido el contenedor de mongoDB en el docker desktop
Entregables nivel 1
N1-1
es solo a nivel de backend por tanto `npm run dev` y tener encendido el contenedor de mongoDB en el docker desktop
N1-2
es solo a nivel de backend por tanto `npm run dev` y tener encendido el contenedor de mongoDB en el docker desktop
